# `app/` (Flutter client)

Flutter app for browsing Reykjavik happy hours. It fetches data from the JSON API generated by `../webservice/`.

## Run

```bash
flutter pub get
flutter run
```

## API endpoint

The base URL is currently hardcoded in `lib/infrastructure/api/bars_api_client.dart`.
